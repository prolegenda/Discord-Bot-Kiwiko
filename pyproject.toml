[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "helixbot"
version = "0.1.0"
description = "Modular event-driven Discord bot platform"
requires-python = ">=3.9"
